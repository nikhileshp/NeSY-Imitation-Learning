usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 36,824
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 20:53:27 11/28/25.  [Using 1,583,226,344 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( diversnotfull(A) )
%   then if ( facingright(A) )
%   | then return 0.221152899494769;  // std dev = 55.600, 13,369.000 (wgt'ed) examples reached here.  /* #neg=8,516 #pos=4,853 */
%   | else return 0.23133122802149633;  // std dev = 53.959, 12,447.000 (wgt'ed) examples reached here.  /* #neg=7,802 #pos=4,645 */
%   else if ( oxygenok(A) )
%   | then return 0.12689893509950712;  // std dev = 44.860, 10,240.000 (wgt'ed) examples reached here.  /* #neg=7,488 #pos=2,752 */
%   | else return -0.11060106490048845;  // std dev = 4.822, 768.000 (wgt'ed) examples reached here.  /* #neg=744 #pos=24 */


% Clauses:

action(A, B, 0.221152899494769) :- 
     diversnotfull(A), 
     facingright(A), 
     !. // Clause #1.

action(A, B, 0.23133122802149633) :- 
     diversnotfull(A), 
     !. // Clause #2.

action(A, B, 0.12689893509950712) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.11060106490048845) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.221152899494769) :-  /* #neg=8,516 #pos=4,853 */ 
   diversnotfull(a),
   facingright(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.23133122802149633) :-  /* #neg=7,802 #pos=4,645 */ 
   diversnotfull(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.12689893509950712) :-  /* #neg=7,488 #pos=2,752 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.11060106490048845) :-  /* #neg=744 #pos=24 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingright(a)
%   oxygenok(a)
%   diversnotfull(a)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 3, [
   facingright(a),
   oxygenok(a),
   diversnotfull(a)]).

%%%%%  WILL-Produced Tree Combined @ 20:54:12 11/28/25.  [Using 2,256,643,744 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( diversnotfull(A) )
%   then if ( facingright(A) )
%   | then return 0.221152899494769;  // std dev = 55.600, 13,369.000 (wgt'ed) examples reached here.  /* #neg=8,516 #pos=4,853 */
%   | else return 0.23133122802149633;  // std dev = 53.959, 12,447.000 (wgt'ed) examples reached here.  /* #neg=7,802 #pos=4,645 */
%   else if ( oxygenok(A) )
%   | then return 0.12689893509950712;  // std dev = 44.860, 10,240.000 (wgt'ed) examples reached here.  /* #neg=7,488 #pos=2,752 */
%   | else return -0.11060106490048845;  // std dev = 4.822, 768.000 (wgt'ed) examples reached here.  /* #neg=744 #pos=24 */


% Clauses:

action(A, B, 0.221152899494769) :- 
     diversnotfull(A), 
     facingright(A), 
     !. // Clause #1.

action(A, B, 0.23133122802149633) :- 
     diversnotfull(A), 
     !. // Clause #2.

action(A, B, 0.12689893509950712) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.11060106490048845) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.221152899494769) :-  /* #neg=8,516 #pos=4,853 */ 
   diversnotfull(a),
   facingright(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.23133122802149633) :-  /* #neg=7,802 #pos=4,645 */ 
   diversnotfull(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.12689893509950712) :-  /* #neg=7,488 #pos=2,752 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.11060106490048845) :-  /* #neg=744 #pos=24 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingright(a)
%   oxygenok(a)
%   diversnotfull(a)
