usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 26,419
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 3:39:49 11/28/25.  [Using 494,903,488 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), facingright(A) )
%   then if ( visiblediver(A, D), samelevelasdiver(A, D) )
%   | then return 0.7358551960708355;  // std dev = 19.154, 3,418.000 (wgt'ed) examples reached here.  /* #neg=418 #pos=3,000 */
%   | else return 0.3476686380579453;  // std dev = 45.676, 8,349.000 (wgt'ed) examples reached here.  /* #neg=4,262 #pos=4,087 */
%   else if ( facingright(A), diversnotfull(A) )
%   | then return 0.2699576047557105;  // std dev = 25.460, 2,676.000 (wgt'ed) examples reached here.  /* #neg=1,574 #pos=1,102 */
%   | else return -0.09099936149367861;  // std dev = 24.042, 11,976.000 (wgt'ed) examples reached here.  /* #neg=11,367 #pos=609 */


% Clauses:

action(A, B, 0.7358551960708355) :- 
     visibleenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     samelevelasdiver(A, D), 
     !. // Clause #1.

action(A, B, 0.3476686380579453) :- 
     visibleenemy(A, C), 
     facingright(A), 
     !. // Clause #2.

action(A, B, 0.2699576047557105) :- 
     facingright(A), 
     diversnotfull(A), 
     !. // Clause #3.

action(A, B, -0.09099936149367861) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.7358551960708355) :-  /* #neg=418 #pos=3,000 */ 
   visibleenemy(a, underscore),
   facingright(a),
   visiblediver(a, uniqueVar1),
   samelevelasdiver(a, uniqueVar1),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.3476686380579453) :-  /* #neg=4,262 #pos=4,087 */ 
   visibleenemy(a, underscore),
   facingright(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.2699576047557105) :-  /* #neg=1,574 #pos=1,102 */ 
   facingright(a),
   diversnotfull(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.09099936149367861) :-  /* #neg=11,367 #pos=609 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingright(a)
%   visiblediver(a, uniqueVar1)
%   diversnotfull(a)
%   samelevelasdiver(a, uniqueVar1)
%   visibleenemy(a, underscore)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 5, [
   facingright(a),
   visiblediver(a, uniqueVar1),
   diversnotfull(a),
   samelevelasdiver(a, uniqueVar1),
   visibleenemy(a, underscore)]).

%%%%%  WILL-Produced Tree Combined @ 3:40:04 11/28/25.  [Using 502,642,560 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), facingright(A) )
%   then if ( visiblediver(A, D), samelevelasdiver(A, D) )
%   | then return 0.7358551960708355;  // std dev = 19.154, 3,418.000 (wgt'ed) examples reached here.  /* #neg=418 #pos=3,000 */
%   | else return 0.3476686380579453;  // std dev = 45.676, 8,349.000 (wgt'ed) examples reached here.  /* #neg=4,262 #pos=4,087 */
%   else if ( facingright(A), visiblediver(A, E) )
%   | then return 0.3529987205072326;  // std dev = 24.134, 2,330.000 (wgt'ed) examples reached here.  /* #neg=1,177 #pos=1,153 */
%   | else return -0.0965662085460041;  // std dev = 23.081, 12,322.000 (wgt'ed) examples reached here.  /* #neg=11,764 #pos=558 */


% Clauses:

action(A, B, 0.7358551960708355) :- 
     visibleenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     samelevelasdiver(A, D), 
     !. // Clause #1.

action(A, B, 0.3476686380579453) :- 
     visibleenemy(A, C), 
     facingright(A), 
     !. // Clause #2.

action(A, B, 0.3529987205072326) :- 
     facingright(A), 
     visiblediver(A, C), 
     !. // Clause #3.

action(A, B, -0.0965662085460041) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.7358551960708355) :-  /* #neg=418 #pos=3,000 */ 
   visibleenemy(a, underscore),
   facingright(a),
   visiblediver(a, uniqueVar2),
   samelevelasdiver(a, uniqueVar2),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.3476686380579453) :-  /* #neg=4,262 #pos=4,087 */ 
   visibleenemy(a, underscore),
   facingright(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.3529987205072326) :-  /* #neg=1,177 #pos=1,153 */ 
   facingright(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.0965662085460041) :-  /* #neg=11,764 #pos=558 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   samelevelasdiver(a, uniqueVar2)
%   visiblediver(a, underscore)
%   visiblediver(a, uniqueVar2)
%   facingright(a)
%   visibleenemy(a, underscore)
