usePrologVariables: true.

% maxTreeDepthInNodes                 = 3
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 100
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 929
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 13:44:44 10/16/25.  [Using 92,151,768 memory cells.]  %%%%%

% FOR fire(A):
%   if ( diversfull(A) )
%   then if ( facingleft(A) )
%   | then if ( visiblediver(A, B) )
%   | | then if ( belowofdiver(A, B) )
%   | | | then return 0.4501660026875221;  // std dev = 2.11e-08, 6.000 (wgt'ed) examples reached here.  /* #neg=5 #pos=1 */
%   | | | else return 0.4501660026875224;  // std dev = 0.000, 30.000 (wgt'ed) examples reached here.  /* #neg=30 */
%   | | else return 0.4501660026875221;  // std dev = 2.11e-08, 6.000 (wgt'ed) examples reached here.  /* #neg=5 #pos=1 */
%   | else if ( visiblemissile(A, C) )
%   | | then return 0.45016600268752216;  // std dev = 0.000, 4.000 (wgt'ed) examples reached here.  /* #neg=4 */
%   | | else if ( visibleenemysubmarine(A, D) )
%   | | | then return 0.4501660026875223;  // std dev = 0.000, 20.000 (wgt'ed) examples reached here.  /* #neg=15 #pos=5 */
%   | | | else return 0.45016600268752216;  // std dev = 1.49e-08, 5.000 (wgt'ed) examples reached here.  /* #neg=5 */
%   else return 0.450166002687524;  // std dev = 0.000, 858.000 (wgt'ed) examples reached here.  /* #neg=551 #pos=307 */


% Clauses:

fire(A, 0.4501660026875221) :- 
     diversfull(A), 
     facingleft(A), 
     visiblediver(A, B), 
     belowofdiver(A, B), 
     !. // Clause #1.

fire(A, 0.4501660026875224) :- 
     diversfull(A), 
     facingleft(A), 
     visiblediver(A, B), 
     !. // Clause #2.

fire(A, 0.4501660026875221) :- 
     diversfull(A), 
     facingleft(A), 
     !. // Clause #3.

fire(A, 0.45016600268752216) :- 
     diversfull(A), 
     visiblemissile(A, B), 
     !. // Clause #4.

fire(A, 0.4501660026875223) :- 
     diversfull(A), 
     visibleenemysubmarine(A, B), 
     !. // Clause #5.

fire(A, 0.45016600268752216) :- 
     diversfull(A), 
     !. // Clause #6.

fire(A, 0.450166002687524) :- !. // Clause #7.


% The flattened versions of these clauses:

flattened_fire(a, 0.4501660026875221) :-  /* #neg=5 #pos=1 */ 
   diversfull(a),
   facingleft(a),
   visiblediver(a, uniqueVar1),
   belowofdiver(a, uniqueVar1),
   !. // Flattened version of clause #1.

flattened_fire(a, 0.4501660026875224) :-  /* #neg=30 */ 
   diversfull(a),
   facingleft(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #2.

flattened_fire(a, 0.4501660026875221) :-  /* #neg=5 #pos=1 */ 
   diversfull(a),
   facingleft(a),
   !. // Flattened version of clause #3.

flattened_fire(a, 0.45016600268752216) :-  /* #neg=4 */ 
   diversfull(a),
   visiblemissile(a, underscore),
   !. // Flattened version of clause #4.

flattened_fire(a, 0.4501660026875223) :-  /* #neg=15 #pos=5 */ 
   diversfull(a),
   visibleenemysubmarine(a, underscore),
   !. // Flattened version of clause #5.

flattened_fire(a, 0.45016600268752216) :-  /* #neg=5 */ 
   diversfull(a),
   !. // Flattened version of clause #6.

flattened_fire(underscore, 0.450166002687524) :-  /* #neg=551 #pos=307 */ 
   !. // Flattened version of clause #7.


% The unique flattened literals:
%   visibleenemysubmarine(a, underscore)
%   visiblediver(a, underscore)
%   belowofdiver(a, uniqueVar1)
%   diversfull(a)
%   visiblemissile(a, underscore)
%   facingleft(a)
%   visiblediver(a, uniqueVar1)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for fire.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
fire(D, Total) :- // A general accessor. 
   fire(D, 1000000, Total), !.
fire(D, Total) :- waitHere("This should not fail", fire(D, Total)).

fire(D, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_fire_tree1(D, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
fire(D, TreesToUse, Total) :- waitHere("This should not fail", fire(D, TreesToUse, Total)).

getScore_fire_tree1(D, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_fire_tree1(D, TreesToUse, Total1) :- fire_tree1(D, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(fire, 7, [
   visibleenemysubmarine(a, underscore),
   visiblediver(a, underscore),
   belowofdiver(a, uniqueVar1),
   diversfull(a),
   visiblemissile(a, underscore),
   facingleft(a),
   visiblediver(a, uniqueVar1)]).

%%%%%  WILL-Produced Tree Combined @ 13:44:45 10/16/25.  [Using 92,282,280 memory cells.]  %%%%%

% FOR fire(A):
%   if ( diversempty(A) )
%   then return 0.4501660026875229;  // std dev = 0.000, 138.000 (wgt'ed) examples reached here.  /* #neg=114 #pos=24 */
%   else return 0.450166002687524;  // std dev = 0.000, 791.000 (wgt'ed) examples reached here.  /* #neg=501 #pos=290 */


% Clauses:

fire(A, 0.4501660026875229) :- 
     diversempty(A), 
     !. // Clause #1.

fire(A, 0.450166002687524) :- !. // Clause #2.


% The flattened versions of these clauses:

flattened_fire(a, 0.4501660026875229) :-  /* #neg=114 #pos=24 */ 
   diversempty(a),
   !. // Flattened version of clause #1.

flattened_fire(underscore, 0.450166002687524) :-  /* #neg=501 #pos=290 */ 
   !. // Flattened version of clause #2.


% The unique flattened literals:
%   diversempty(a)
