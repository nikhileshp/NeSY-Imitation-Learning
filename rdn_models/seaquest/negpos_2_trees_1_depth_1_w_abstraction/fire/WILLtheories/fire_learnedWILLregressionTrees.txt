usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 100
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 946
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 21:07:29 10/20/25.  [Using 59,548,800 memory cells.]  %%%%%

% FOR fire(A):
%   if ( visibleenemy(A, B), samelevelasenemy(A, B) )
%   then if ( diversnotfull(A), facingleft(A) )
%   | then return 0.6605441446803479;  // std dev = 5.146, 167.000 (wgt'ed) examples reached here.  /* #neg=33 #pos=134 */
%   | else return 0.4056349686190638;  // std dev = 6.659, 179.000 (wgt'ed) examples reached here.  /* #neg=81 #pos=98 */
%   else if ( visiblediver(A, C) )
%   | then return -0.08769944035175155;  // std dev = 3.767, 277.000 (wgt'ed) examples reached here.  /* #neg=262 #pos=15 */
%   | else return 0.06557927565678856;  // std dev = 7.287, 323.000 (wgt'ed) examples reached here.  /* #neg=256 #pos=67 */


% Clauses:

fire(A, 0.6605441446803479) :- 
     visibleenemy(A, B), 
     samelevelasenemy(A, B), 
     diversnotfull(A), 
     facingleft(A), 
     !. // Clause #1.

fire(A, 0.4056349686190638) :- 
     visibleenemy(A, B), 
     samelevelasenemy(A, B), 
     !. // Clause #2.

fire(A, -0.08769944035175155) :- 
     visiblediver(A, B), 
     !. // Clause #3.

fire(A, 0.06557927565678856) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_fire(a, 0.6605441446803479) :-  /* #neg=33 #pos=134 */ 
   visibleenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar1),
   diversnotfull(a),
   facingleft(a),
   !. // Flattened version of clause #1.

flattened_fire(a, 0.4056349686190638) :-  /* #neg=81 #pos=98 */ 
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar2),
   !. // Flattened version of clause #2.

flattened_fire(a, -0.08769944035175155) :-  /* #neg=262 #pos=15 */ 
   visiblediver(a, underscore),
   !. // Flattened version of clause #3.

flattened_fire(underscore, 0.06557927565678856) :-  /* #neg=256 #pos=67 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   samelevelasenemy(a, uniqueVar2)
%   visibleenemy(a, uniqueVar1)
%   visibleenemy(a, uniqueVar2)
%   samelevelasenemy(a, uniqueVar1)
%   facingleft(a)
%   visiblediver(a, underscore)
%   diversnotfull(a)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for fire.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
fire(D, Total) :- // A general accessor. 
   fire(D, 1000000, Total), !.
fire(D, Total) :- waitHere("This should not fail", fire(D, Total)).

fire(D, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_fire_tree1(D, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
fire(D, TreesToUse, Total) :- waitHere("This should not fail", fire(D, TreesToUse, Total)).

getScore_fire_tree1(D, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_fire_tree1(D, TreesToUse, Total1) :- fire_tree1(D, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(fire, 7, [
   samelevelasenemy(a, uniqueVar2),
   visibleenemy(a, uniqueVar1),
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar1),
   facingleft(a),
   visiblediver(a, underscore),
   diversnotfull(a)]).

%%%%%  WILL-Produced Tree Combined @ 21:07:31 10/20/25.  [Using 59,337,648 memory cells.]  %%%%%

% FOR fire(A):
%   if ( visibleenemy(A, B), samelevelasenemy(A, B) )
%   then if ( diversnotfull(A), facingleft(A) )
%   | then return 0.6605441446803479;  // std dev = 5.146, 167.000 (wgt'ed) examples reached here.  /* #neg=33 #pos=134 */
%   | else return 0.4056349686190638;  // std dev = 6.659, 179.000 (wgt'ed) examples reached here.  /* #neg=81 #pos=98 */
%   else if ( visiblediver(A, C) )
%   | then return -0.08769944035175155;  // std dev = 3.767, 277.000 (wgt'ed) examples reached here.  /* #neg=262 #pos=15 */
%   | else return 0.06557927565678856;  // std dev = 7.287, 323.000 (wgt'ed) examples reached here.  /* #neg=256 #pos=67 */


% Clauses:

fire(A, 0.6605441446803479) :- 
     visibleenemy(A, B), 
     samelevelasenemy(A, B), 
     diversnotfull(A), 
     facingleft(A), 
     !. // Clause #1.

fire(A, 0.4056349686190638) :- 
     visibleenemy(A, B), 
     samelevelasenemy(A, B), 
     !. // Clause #2.

fire(A, -0.08769944035175155) :- 
     visiblediver(A, B), 
     !. // Clause #3.

fire(A, 0.06557927565678856) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_fire(a, 0.6605441446803479) :-  /* #neg=33 #pos=134 */ 
   visibleenemy(a, uniqueVar3),
   samelevelasenemy(a, uniqueVar3),
   diversnotfull(a),
   facingleft(a),
   !. // Flattened version of clause #1.

flattened_fire(a, 0.4056349686190638) :-  /* #neg=81 #pos=98 */ 
   visibleenemy(a, uniqueVar4),
   samelevelasenemy(a, uniqueVar4),
   !. // Flattened version of clause #2.

flattened_fire(a, -0.08769944035175155) :-  /* #neg=262 #pos=15 */ 
   visiblediver(a, underscore),
   !. // Flattened version of clause #3.

flattened_fire(underscore, 0.06557927565678856) :-  /* #neg=256 #pos=67 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visibleenemy(a, uniqueVar3)
%   samelevelasenemy(a, uniqueVar3)
%   visibleenemy(a, uniqueVar4)
%   facingleft(a)
%   samelevelasenemy(a, uniqueVar4)
%   visiblediver(a, underscore)
%   diversnotfull(a)
