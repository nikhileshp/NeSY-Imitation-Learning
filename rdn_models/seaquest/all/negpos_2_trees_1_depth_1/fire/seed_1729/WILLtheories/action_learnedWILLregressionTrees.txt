usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 16,176
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 3:45:05 11/28/25.  [Using 500,431,528 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), samelevelasenemy(A, C) )
%   then if ( facingright(A), visiblediver(A, D) )
%   | then return 0.4310739955346404;  // std dev = 17.426, 1,241.000 (wgt'ed) examples reached here.  /* #neg=530 #pos=711 */
%   | else return 0.5876823243143718;  // std dev = 26.574, 3,579.000 (wgt'ed) examples reached here.  /* #neg=968 #pos=2,611 */
%   else if ( visibleenemysubmarine(A, E), belowofsubmarine(A, E) )
%   | then return 0.14057748236823706;  // std dev = 34.617, 5,913.000 (wgt'ed) examples reached here.  /* #neg=4,243 #pos=1,670 */
%   | else return -0.07240406875865732;  // std dev = 18.755, 5,443.000 (wgt'ed) examples reached here.  /* #neg=5,065 #pos=378 */


% Clauses:

action(A, B, 0.4310739955346404) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     !. // Clause #1.

action(A, B, 0.5876823243143718) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #2.

action(A, B, 0.14057748236823706) :- 
     visibleenemysubmarine(A, C), 
     belowofsubmarine(A, C), 
     !. // Clause #3.

action(A, B, -0.07240406875865732) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4310739955346404) :-  /* #neg=530 #pos=711 */ 
   visibleenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar1),
   facingright(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.5876823243143718) :-  /* #neg=968 #pos=2,611 */ 
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar2),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.14057748236823706) :-  /* #neg=4,243 #pos=1,670 */ 
   visibleenemysubmarine(a, uniqueVar3),
   belowofsubmarine(a, uniqueVar3),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.07240406875865732) :-  /* #neg=5,065 #pos=378 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visiblediver(a, underscore)
%   visibleenemysubmarine(a, uniqueVar3)
%   belowofsubmarine(a, uniqueVar3)
%   samelevelasenemy(a, uniqueVar2)
%   visibleenemy(a, uniqueVar2)
%   facingright(a)
%   samelevelasenemy(a, uniqueVar1)
%   visibleenemy(a, uniqueVar1)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 8, [
   visiblediver(a, underscore),
   visibleenemysubmarine(a, uniqueVar3),
   belowofsubmarine(a, uniqueVar3),
   samelevelasenemy(a, uniqueVar2),
   visibleenemy(a, uniqueVar2),
   facingright(a),
   samelevelasenemy(a, uniqueVar1),
   visibleenemy(a, uniqueVar1)]).

%%%%%  WILL-Produced Tree Combined @ 3:45:12 11/28/25.  [Using 499,835,048 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), samelevelasenemy(A, C) )
%   then if ( facingright(A), rightofenemy(A, C) )
%   | then return 0.25760545683863867;  // std dev = 9.396, 368.000 (wgt'ed) examples reached here.  /* #neg=221 #pos=147 */
%   | else return 0.5713115586394893;  // std dev = 30.178, 4,452.000 (wgt'ed) examples reached here.  /* #neg=1,277 #pos=3,175 */
%   else if ( visibleenemysubmarine(A, D), belowofsubmarine(A, D) )
%   | then return 0.14057748236823706;  // std dev = 34.617, 5,913.000 (wgt'ed) examples reached here.  /* #neg=4,243 #pos=1,670 */
%   | else return -0.07240406875865732;  // std dev = 18.755, 5,443.000 (wgt'ed) examples reached here.  /* #neg=5,065 #pos=378 */


% Clauses:

action(A, B, 0.25760545683863867) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     facingright(A), 
     rightofenemy(A, C), 
     !. // Clause #1.

action(A, B, 0.5713115586394893) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #2.

action(A, B, 0.14057748236823706) :- 
     visibleenemysubmarine(A, C), 
     belowofsubmarine(A, C), 
     !. // Clause #3.

action(A, B, -0.07240406875865732) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.25760545683863867) :-  /* #neg=221 #pos=147 */ 
   visibleenemy(a, uniqueVar4),
   samelevelasenemy(a, uniqueVar4),
   facingright(a),
   rightofenemy(a, uniqueVar4),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.5713115586394893) :-  /* #neg=1,277 #pos=3,175 */ 
   visibleenemy(a, uniqueVar5),
   samelevelasenemy(a, uniqueVar5),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.14057748236823706) :-  /* #neg=4,243 #pos=1,670 */ 
   visibleenemysubmarine(a, uniqueVar6),
   belowofsubmarine(a, uniqueVar6),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.07240406875865732) :-  /* #neg=5,065 #pos=378 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   samelevelasenemy(a, uniqueVar5)
%   visibleenemy(a, uniqueVar4)
%   visibleenemy(a, uniqueVar5)
%   facingright(a)
%   rightofenemy(a, uniqueVar4)
%   visibleenemysubmarine(a, uniqueVar6)
%   belowofsubmarine(a, uniqueVar6)
%   samelevelasenemy(a, uniqueVar4)
