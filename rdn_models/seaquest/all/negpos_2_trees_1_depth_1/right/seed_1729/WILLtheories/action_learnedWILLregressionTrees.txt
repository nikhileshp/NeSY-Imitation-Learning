usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 26,229
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 3:47:48 11/28/25.  [Using 503,317,632 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), facingright(A) )
%   then if ( visiblediver(A, D), samelevelasdiver(A, D) )
%   | then return 0.7292290744723295;  // std dev = 19.666, 3,444.000 (wgt'ed) examples reached here.  /* #neg=444 #pos=3,000 */
%   | else return 0.3458577656484332;  // std dev = 45.757, 8,380.000 (wgt'ed) examples reached here.  /* #neg=4,293 #pos=4,087 */
%   else if ( facingright(A), visiblediver(A, E) )
%   | then return 0.3805775712571874;  // std dev = 23.466, 2,207.000 (wgt'ed) examples reached here.  /* #neg=1,054 #pos=1,153 */
%   | else return -0.0961058607686671;  // std dev = 23.075, 12,198.000 (wgt'ed) examples reached here.  /* #neg=11,640 #pos=558 */


% Clauses:

action(A, B, 0.7292290744723295) :- 
     visibleenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     samelevelasdiver(A, D), 
     !. // Clause #1.

action(A, B, 0.3458577656484332) :- 
     visibleenemy(A, C), 
     facingright(A), 
     !. // Clause #2.

action(A, B, 0.3805775712571874) :- 
     facingright(A), 
     visiblediver(A, C), 
     !. // Clause #3.

action(A, B, -0.0961058607686671) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.7292290744723295) :-  /* #neg=444 #pos=3,000 */ 
   visibleenemy(a, underscore),
   facingright(a),
   visiblediver(a, uniqueVar1),
   samelevelasdiver(a, uniqueVar1),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.3458577656484332) :-  /* #neg=4,293 #pos=4,087 */ 
   visibleenemy(a, underscore),
   facingright(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.3805775712571874) :-  /* #neg=1,054 #pos=1,153 */ 
   facingright(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.0961058607686671) :-  /* #neg=11,640 #pos=558 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visiblediver(a, uniqueVar1)
%   visibleenemy(a, underscore)
%   visiblediver(a, underscore)
%   facingright(a)
%   samelevelasdiver(a, uniqueVar1)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 5, [
   visiblediver(a, uniqueVar1),
   visibleenemy(a, underscore),
   visiblediver(a, underscore),
   facingright(a),
   samelevelasdiver(a, uniqueVar1)]).

%%%%%  WILL-Produced Tree Combined @ 3:48:00 11/28/25.  [Using 503,327,368 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), facingright(A) )
%   then if ( visiblediver(A, D), samelevelasdiver(A, D) )
%   | then return 0.7292290744723295;  // std dev = 19.666, 3,444.000 (wgt'ed) examples reached here.  /* #neg=444 #pos=3,000 */
%   | else return 0.3458577656484332;  // std dev = 45.757, 8,380.000 (wgt'ed) examples reached here.  /* #neg=4,293 #pos=4,087 */
%   else if ( facingright(A), visiblediver(A, E) )
%   | then return 0.3805775712571874;  // std dev = 23.466, 2,207.000 (wgt'ed) examples reached here.  /* #neg=1,054 #pos=1,153 */
%   | else return -0.0961058607686671;  // std dev = 23.075, 12,198.000 (wgt'ed) examples reached here.  /* #neg=11,640 #pos=558 */


% Clauses:

action(A, B, 0.7292290744723295) :- 
     visibleenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     samelevelasdiver(A, D), 
     !. // Clause #1.

action(A, B, 0.3458577656484332) :- 
     visibleenemy(A, C), 
     facingright(A), 
     !. // Clause #2.

action(A, B, 0.3805775712571874) :- 
     facingright(A), 
     visiblediver(A, C), 
     !. // Clause #3.

action(A, B, -0.0961058607686671) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.7292290744723295) :-  /* #neg=444 #pos=3,000 */ 
   visibleenemy(a, underscore),
   facingright(a),
   visiblediver(a, uniqueVar2),
   samelevelasdiver(a, uniqueVar2),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.3458577656484332) :-  /* #neg=4,293 #pos=4,087 */ 
   visibleenemy(a, underscore),
   facingright(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.3805775712571874) :-  /* #neg=1,054 #pos=1,153 */ 
   facingright(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.0961058607686671) :-  /* #neg=11,640 #pos=558 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visiblediver(a, uniqueVar2)
%   visibleenemy(a, underscore)
%   visiblediver(a, underscore)
%   samelevelasdiver(a, uniqueVar2)
%   facingright(a)
