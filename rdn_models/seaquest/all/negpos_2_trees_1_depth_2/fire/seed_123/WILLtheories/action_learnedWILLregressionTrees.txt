usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 16,077
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 3:52:36 11/28/25.  [Using 500,293,136 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), samelevelasenemy(A, C) )
%   then if ( facingright(A), visiblediver(A, D) )
%   | then return 0.4219871587315455;  // std dev = 17.610, 1,261.000 (wgt'ed) examples reached here.  /* #neg=550 #pos=711 */
%   | else return 0.5961364986947589;  // std dev = 26.156, 3,538.000 (wgt'ed) examples reached here.  /* #neg=927 #pos=2,611 */
%   else if ( visibleenemysubmarine(A, E), belowofsubmarine(A, E) )
%   | then return 0.14308324665043703;  // std dev = 34.557, 5,861.000 (wgt'ed) examples reached here.  /* #neg=4,191 #pos=1,670 */
%   | else return -0.07207074368948845;  // std dev = 18.752, 5,417.000 (wgt'ed) examples reached here.  /* #neg=5,039 #pos=378 */


% Clauses:

action(A, B, 0.4219871587315455) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     !. // Clause #1.

action(A, B, 0.5961364986947589) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #2.

action(A, B, 0.14308324665043703) :- 
     visibleenemysubmarine(A, C), 
     belowofsubmarine(A, C), 
     !. // Clause #3.

action(A, B, -0.07207074368948845) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4219871587315455) :-  /* #neg=550 #pos=711 */ 
   visibleenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar1),
   facingright(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.5961364986947589) :-  /* #neg=927 #pos=2,611 */ 
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar2),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.14308324665043703) :-  /* #neg=4,191 #pos=1,670 */ 
   visibleenemysubmarine(a, uniqueVar3),
   belowofsubmarine(a, uniqueVar3),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.07207074368948845) :-  /* #neg=5,039 #pos=378 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visibleenemy(a, uniqueVar1)
%   samelevelasenemy(a, uniqueVar1)
%   visiblediver(a, underscore)
%   belowofsubmarine(a, uniqueVar3)
%   visibleenemysubmarine(a, uniqueVar3)
%   facingright(a)
%   visibleenemy(a, uniqueVar2)
%   samelevelasenemy(a, uniqueVar2)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 8, [
   visibleenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar1),
   visiblediver(a, underscore),
   belowofsubmarine(a, uniqueVar3),
   visibleenemysubmarine(a, uniqueVar3),
   facingright(a),
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar2)]).

%%%%%  WILL-Produced Tree Combined @ 3:52:45 11/28/25.  [Using 500,032,648 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), samelevelasenemy(A, C) )
%   then if ( facingright(A), visiblediver(A, D) )
%   | then return 0.4219871587315455;  // std dev = 17.610, 1,261.000 (wgt'ed) examples reached here.  /* #neg=550 #pos=711 */
%   | else return 0.5961364986947589;  // std dev = 26.156, 3,538.000 (wgt'ed) examples reached here.  /* #neg=927 #pos=2,611 */
%   else if ( visibleenemysubmarine(A, E), belowofsubmarine(A, E) )
%   | then return 0.14308324665043703;  // std dev = 34.557, 5,861.000 (wgt'ed) examples reached here.  /* #neg=4,191 #pos=1,670 */
%   | else return -0.07207074368948845;  // std dev = 18.752, 5,417.000 (wgt'ed) examples reached here.  /* #neg=5,039 #pos=378 */


% Clauses:

action(A, B, 0.4219871587315455) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     facingright(A), 
     visiblediver(A, D), 
     !. // Clause #1.

action(A, B, 0.5961364986947589) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #2.

action(A, B, 0.14308324665043703) :- 
     visibleenemysubmarine(A, C), 
     belowofsubmarine(A, C), 
     !. // Clause #3.

action(A, B, -0.07207074368948845) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4219871587315455) :-  /* #neg=550 #pos=711 */ 
   visibleenemy(a, uniqueVar4),
   samelevelasenemy(a, uniqueVar4),
   facingright(a),
   visiblediver(a, underscore),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.5961364986947589) :-  /* #neg=927 #pos=2,611 */ 
   visibleenemy(a, uniqueVar5),
   samelevelasenemy(a, uniqueVar5),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.14308324665043703) :-  /* #neg=4,191 #pos=1,670 */ 
   visibleenemysubmarine(a, uniqueVar6),
   belowofsubmarine(a, uniqueVar6),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.07207074368948845) :-  /* #neg=5,039 #pos=378 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visibleenemy(a, uniqueVar4)
%   belowofsubmarine(a, uniqueVar6)
%   visiblediver(a, underscore)
%   samelevelasenemy(a, uniqueVar5)
%   visibleenemy(a, uniqueVar5)
%   facingright(a)
%   visibleenemysubmarine(a, uniqueVar6)
%   samelevelasenemy(a, uniqueVar4)
