usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 26,419
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 22:50:32 11/28/25.  [Using 1,386,835,128 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( facingright(A) )
%   then if ( diversnotfull(A) )
%   | then return 0.4418467482210616;  // std dev = 55.278, 12,575.000 (wgt'ed) examples reached here.  /* #neg=5,235 #pos=7,340 */
%   | else return 0.15793818694355355;  // std dev = 28.228, 3,796.000 (wgt'ed) examples reached here.  /* #neg=2,658 #pos=1,138 */
%   else if ( diversnotfull(A) )
%   | then return -0.10039473936890264;  // std dev = 16.117, 6,537.000 (wgt'ed) examples reached here.  /* #neg=6,266 #pos=271 */
%   | else return -0.127894927048028;  // std dev = 6.951, 3,511.000 (wgt'ed) examples reached here.  /* #neg=3,462 #pos=49 */


% Clauses:

action(A, B, 0.4418467482210616) :- 
     facingright(A), 
     diversnotfull(A), 
     !. // Clause #1.

action(A, B, 0.15793818694355355) :- 
     facingright(A), 
     !. // Clause #2.

action(A, B, -0.10039473936890264) :- 
     diversnotfull(A), 
     !. // Clause #3.

action(A, B, -0.127894927048028) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4418467482210616) :-  /* #neg=5,235 #pos=7,340 */ 
   facingright(a),
   diversnotfull(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.15793818694355355) :-  /* #neg=2,658 #pos=1,138 */ 
   facingright(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, -0.10039473936890264) :-  /* #neg=6,266 #pos=271 */ 
   diversnotfull(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.127894927048028) :-  /* #neg=3,462 #pos=49 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   diversnotfull(a)
%   facingright(a)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 2, [
   diversnotfull(a),
   facingright(a)]).

%%%%%  WILL-Produced Tree Combined @ 22:51:04 11/28/25.  [Using 1,884,067,872 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( facingright(A) )
%   then if ( diversnotfull(A) )
%   | then return 0.4418467482210616;  // std dev = 55.278, 12,575.000 (wgt'ed) examples reached here.  /* #neg=5,235 #pos=7,340 */
%   | else return 0.15793818694355355;  // std dev = 28.228, 3,796.000 (wgt'ed) examples reached here.  /* #neg=2,658 #pos=1,138 */
%   else if ( diversnotfull(A) )
%   | then return -0.10039473936890264;  // std dev = 16.117, 6,537.000 (wgt'ed) examples reached here.  /* #neg=6,266 #pos=271 */
%   | else return -0.127894927048028;  // std dev = 6.951, 3,511.000 (wgt'ed) examples reached here.  /* #neg=3,462 #pos=49 */


% Clauses:

action(A, B, 0.4418467482210616) :- 
     facingright(A), 
     diversnotfull(A), 
     !. // Clause #1.

action(A, B, 0.15793818694355355) :- 
     facingright(A), 
     !. // Clause #2.

action(A, B, -0.10039473936890264) :- 
     diversnotfull(A), 
     !. // Clause #3.

action(A, B, -0.127894927048028) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4418467482210616) :-  /* #neg=5,235 #pos=7,340 */ 
   facingright(a),
   diversnotfull(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.15793818694355355) :-  /* #neg=2,658 #pos=1,138 */ 
   facingright(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, -0.10039473936890264) :-  /* #neg=6,266 #pos=271 */ 
   diversnotfull(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.127894927048028) :-  /* #neg=3,462 #pos=49 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   diversnotfull(a)
%   facingright(a)
