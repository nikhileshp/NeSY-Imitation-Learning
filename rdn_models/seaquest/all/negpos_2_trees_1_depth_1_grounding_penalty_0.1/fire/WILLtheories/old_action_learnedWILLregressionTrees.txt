usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 16,237
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 17:23:55 11/25/25.  [Using 1,217,680,720 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), samelevelasenemy(A, C) )
%   then if ( diversnotfull(A) )
%   | then return 0.5612587504542205;  // std dev = 29.312, 4,116.000 (wgt'ed) examples reached here.  /* #neg=1,222 #pos=2,894 */
%   | else return 0.481147479495432;  // std dev = 12.703, 687.000 (wgt'ed) examples reached here.  /* #neg=259 #pos=428 */
%   else if ( oxygenok(A) )
%   | then return 0.04149575642852429;  // std dev = 40.806, 11,121.000 (wgt'ed) examples reached here.  /* #neg=9,082 #pos=2,039 */
%   | else return -0.1130970712902645;  // std dev = 2.957, 313.000 (wgt'ed) examples reached here.  /* #neg=304 #pos=9 */


% Clauses:

action(A, B, 0.5612587504542205) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     diversnotfull(A), 
     !. // Clause #1.

action(A, B, 0.481147479495432) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #2.

action(A, B, 0.04149575642852429) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.1130970712902645) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.5612587504542205) :-  /* #neg=1,222 #pos=2,894 */ 
   visibleenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar1),
   diversnotfull(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.481147479495432) :-  /* #neg=259 #pos=428 */ 
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar2),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.04149575642852429) :-  /* #neg=9,082 #pos=2,039 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.1130970712902645) :-  /* #neg=304 #pos=9 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   oxygenok(a)
%   visibleenemy(a, uniqueVar1)
%   samelevelasenemy(a, uniqueVar1)
%   samelevelasenemy(a, uniqueVar2)
%   diversnotfull(a)
%   visibleenemy(a, uniqueVar2)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 6, [
   oxygenok(a),
   visibleenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar2),
   diversnotfull(a),
   visibleenemy(a, uniqueVar2)]).

%%%%%  WILL-Produced Tree Combined @ 17:24:18 11/25/25.  [Using 1,559,086,432 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( visibleenemy(A, C), samelevelasenemy(A, C) )
%   then if ( diversnotfull(A) )
%   | then return 0.5612587504542205;  // std dev = 29.312, 4,116.000 (wgt'ed) examples reached here.  /* #neg=1,222 #pos=2,894 */
%   | else return 0.481147479495432;  // std dev = 12.703, 687.000 (wgt'ed) examples reached here.  /* #neg=259 #pos=428 */
%   else if ( oxygenok(A) )
%   | then return 0.04149575642852429;  // std dev = 40.806, 11,121.000 (wgt'ed) examples reached here.  /* #neg=9,082 #pos=2,039 */
%   | else return -0.1130970712902645;  // std dev = 2.957, 313.000 (wgt'ed) examples reached here.  /* #neg=304 #pos=9 */


% Clauses:

action(A, B, 0.5612587504542205) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     diversnotfull(A), 
     !. // Clause #1.

action(A, B, 0.481147479495432) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #2.

action(A, B, 0.04149575642852429) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.1130970712902645) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.5612587504542205) :-  /* #neg=1,222 #pos=2,894 */ 
   visibleenemy(a, uniqueVar3),
   samelevelasenemy(a, uniqueVar3),
   diversnotfull(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.481147479495432) :-  /* #neg=259 #pos=428 */ 
   visibleenemy(a, uniqueVar4),
   samelevelasenemy(a, uniqueVar4),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.04149575642852429) :-  /* #neg=9,082 #pos=2,039 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.1130970712902645) :-  /* #neg=304 #pos=9 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   oxygenok(a)
%   visibleenemy(a, uniqueVar4)
%   diversnotfull(a)
%   samelevelasenemy(a, uniqueVar3)
%   visibleenemy(a, uniqueVar3)
%   samelevelasenemy(a, uniqueVar4)
