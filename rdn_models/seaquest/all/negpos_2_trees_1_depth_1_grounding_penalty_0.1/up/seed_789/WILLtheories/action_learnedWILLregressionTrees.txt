usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 36,827
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 22:46:08 11/28/25.  [Using 1,570,269,480 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( diversnotfull(A) )
%   then if ( facingright(A) )
%   | then return 0.22610708204015886;  // std dev = 55.383, 13,189.000 (wgt'ed) examples reached here.  /* #neg=8,336 #pos=4,853 */
%   | else return 0.23316911917830574;  // std dev = 53.880, 12,386.000 (wgt'ed) examples reached here.  /* #neg=7,741 #pos=4,645 */
%   else if ( oxygenok(A) )
%   | then return 0.12152342069621494;  // std dev = 45.024, 10,449.000 (wgt'ed) examples reached here.  /* #neg=7,697 #pos=2,752 */
%   | else return -0.11196314460160944;  // std dev = 4.825, 803.000 (wgt'ed) examples reached here.  /* #neg=779 #pos=24 */


% Clauses:

action(A, B, 0.22610708204015886) :- 
     diversnotfull(A), 
     facingright(A), 
     !. // Clause #1.

action(A, B, 0.23316911917830574) :- 
     diversnotfull(A), 
     !. // Clause #2.

action(A, B, 0.12152342069621494) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.11196314460160944) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.22610708204015886) :-  /* #neg=8,336 #pos=4,853 */ 
   diversnotfull(a),
   facingright(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.23316911917830574) :-  /* #neg=7,741 #pos=4,645 */ 
   diversnotfull(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.12152342069621494) :-  /* #neg=7,697 #pos=2,752 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.11196314460160944) :-  /* #neg=779 #pos=24 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingright(a)
%   oxygenok(a)
%   diversnotfull(a)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 3, [
   facingright(a),
   oxygenok(a),
   diversnotfull(a)]).

%%%%%  WILL-Produced Tree Combined @ 22:46:39 11/28/25.  [Using 2,240,683,720 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( diversnotfull(A) )
%   then if ( facingright(A) )
%   | then return 0.22610708204015886;  // std dev = 55.383, 13,189.000 (wgt'ed) examples reached here.  /* #neg=8,336 #pos=4,853 */
%   | else return 0.23316911917830574;  // std dev = 53.880, 12,386.000 (wgt'ed) examples reached here.  /* #neg=7,741 #pos=4,645 */
%   else if ( oxygenok(A) )
%   | then return 0.12152342069621494;  // std dev = 45.024, 10,449.000 (wgt'ed) examples reached here.  /* #neg=7,697 #pos=2,752 */
%   | else return -0.11196314460160944;  // std dev = 4.825, 803.000 (wgt'ed) examples reached here.  /* #neg=779 #pos=24 */


% Clauses:

action(A, B, 0.22610708204015886) :- 
     diversnotfull(A), 
     facingright(A), 
     !. // Clause #1.

action(A, B, 0.23316911917830574) :- 
     diversnotfull(A), 
     !. // Clause #2.

action(A, B, 0.12152342069621494) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.11196314460160944) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.22610708204015886) :-  /* #neg=8,336 #pos=4,853 */ 
   diversnotfull(a),
   facingright(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.23316911917830574) :-  /* #neg=7,741 #pos=4,645 */ 
   diversnotfull(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.12152342069621494) :-  /* #neg=7,697 #pos=2,752 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.11196314460160944) :-  /* #neg=779 #pos=24 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingright(a)
%   oxygenok(a)
%   diversnotfull(a)
