usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 36,584
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 22:21:08 11/28/25.  [Using 1,587,182,440 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( diversnotfull(A) )
%   then if ( facingright(A) )
%   | then return 0.22613498302188018;  // std dev = 55.382, 13,188.000 (wgt'ed) examples reached here.  /* #neg=8,335 #pos=4,853 */
%   | else return 0.23490292974627489;  // std dev = 53.805, 12,329.000 (wgt'ed) examples reached here.  /* #neg=7,684 #pos=4,645 */
%   else if ( oxygenok(A) )
%   | then return 0.12504836289506527;  // std dev = 44.917, 10,311.000 (wgt'ed) examples reached here.  /* #neg=7,559 #pos=2,752 */
%   | else return -0.11010503315445663;  // std dev = 4.821, 756.000 (wgt'ed) examples reached here.  /* #neg=732 #pos=24 */


% Clauses:

action(A, B, 0.22613498302188018) :- 
     diversnotfull(A), 
     facingright(A), 
     !. // Clause #1.

action(A, B, 0.23490292974627489) :- 
     diversnotfull(A), 
     !. // Clause #2.

action(A, B, 0.12504836289506527) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.11010503315445663) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.22613498302188018) :-  /* #neg=8,335 #pos=4,853 */ 
   diversnotfull(a),
   facingright(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.23490292974627489) :-  /* #neg=7,684 #pos=4,645 */ 
   diversnotfull(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.12504836289506527) :-  /* #neg=7,559 #pos=2,752 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.11010503315445663) :-  /* #neg=732 #pos=24 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   diversnotfull(a)
%   facingright(a)
%   oxygenok(a)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 3, [
   diversnotfull(a),
   facingright(a),
   oxygenok(a)]).

%%%%%  WILL-Produced Tree Combined @ 22:21:52 11/28/25.  [Using 2,254,735,328 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( diversnotfull(A) )
%   then if ( facingright(A) )
%   | then return 0.22613498302188018;  // std dev = 55.382, 13,188.000 (wgt'ed) examples reached here.  /* #neg=8,335 #pos=4,853 */
%   | else return 0.23490292974627489;  // std dev = 53.805, 12,329.000 (wgt'ed) examples reached here.  /* #neg=7,684 #pos=4,645 */
%   else if ( oxygenok(A) )
%   | then return 0.12504836289506527;  // std dev = 44.917, 10,311.000 (wgt'ed) examples reached here.  /* #neg=7,559 #pos=2,752 */
%   | else return -0.11010503315445663;  // std dev = 4.821, 756.000 (wgt'ed) examples reached here.  /* #neg=732 #pos=24 */


% Clauses:

action(A, B, 0.22613498302188018) :- 
     diversnotfull(A), 
     facingright(A), 
     !. // Clause #1.

action(A, B, 0.23490292974627489) :- 
     diversnotfull(A), 
     !. // Clause #2.

action(A, B, 0.12504836289506527) :- 
     oxygenok(A), 
     !. // Clause #3.

action(A, B, -0.11010503315445663) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.22613498302188018) :-  /* #neg=8,335 #pos=4,853 */ 
   diversnotfull(a),
   facingright(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.23490292974627489) :-  /* #neg=7,684 #pos=4,645 */ 
   diversnotfull(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, 0.12504836289506527) :-  /* #neg=7,559 #pos=2,752 */ 
   oxygenok(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.11010503315445663) :-  /* #neg=732 #pos=24 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   diversnotfull(a)
%   facingright(a)
%   oxygenok(a)
