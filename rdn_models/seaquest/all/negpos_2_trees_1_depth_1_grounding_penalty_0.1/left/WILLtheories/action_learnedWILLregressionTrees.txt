usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 26,577
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 17:45:56 11/25/25.  [Using 1,378,701,856 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( facingleft(A) )
%   then if ( visiblediver(A, C), samelevelasdiver(A, C) )
%   | then return 0.7054345769056969;  // std dev = 22.585, 3,942.000 (wgt'ed) examples reached here.  /* #neg=602 #pos=3,340 */
%   | else return 0.28370242504373566;  // std dev = 53.786, 11,834.000 (wgt'ed) examples reached here.  /* #neg=6,798 #pos=5,036 */
%   else if ( visibleenemy(A, D), samelevelasenemy(A, D) )
%   | then return -0.053141387481134846;  // std dev = 11.846, 1,736.000 (wgt'ed) examples reached here.  /* #neg=1,582 #pos=154 */
%   | else return -0.10655045817131352;  // std dev = 17.570, 9,065.000 (wgt'ed) examples reached here.  /* #neg=8,745 #pos=320 */


% Clauses:

action(A, B, 0.7054345769056969) :- 
     facingleft(A), 
     visiblediver(A, C), 
     samelevelasdiver(A, C), 
     !. // Clause #1.

action(A, B, 0.28370242504373566) :- 
     facingleft(A), 
     !. // Clause #2.

action(A, B, -0.053141387481134846) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #3.

action(A, B, -0.10655045817131352) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.7054345769056969) :-  /* #neg=602 #pos=3,340 */ 
   facingleft(a),
   visiblediver(a, uniqueVar1),
   samelevelasdiver(a, uniqueVar1),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.28370242504373566) :-  /* #neg=6,798 #pos=5,036 */ 
   facingleft(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, -0.053141387481134846) :-  /* #neg=1,582 #pos=154 */ 
   visibleenemy(a, uniqueVar2),
   samelevelasenemy(a, uniqueVar2),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.10655045817131352) :-  /* #neg=8,745 #pos=320 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   samelevelasdiver(a, uniqueVar1)
%   facingleft(a)
%   visiblediver(a, uniqueVar1)
%   samelevelasenemy(a, uniqueVar2)
%   visibleenemy(a, uniqueVar2)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 5, [
   samelevelasdiver(a, uniqueVar1),
   facingleft(a),
   visiblediver(a, uniqueVar1),
   samelevelasenemy(a, uniqueVar2),
   visibleenemy(a, uniqueVar2)]).

%%%%%  WILL-Produced Tree Combined @ 17:46:18 11/25/25.  [Using 1,889,147,456 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( facingleft(A) )
%   then if ( visiblediver(A, C), samelevelasdiver(A, C) )
%   | then return 0.7054345769056969;  // std dev = 22.585, 3,942.000 (wgt'ed) examples reached here.  /* #neg=602 #pos=3,340 */
%   | else return 0.28370242504373566;  // std dev = 53.786, 11,834.000 (wgt'ed) examples reached here.  /* #neg=6,798 #pos=5,036 */
%   else if ( visibleenemy(A, D), samelevelasenemy(A, D) )
%   | then return -0.053141387481134846;  // std dev = 11.846, 1,736.000 (wgt'ed) examples reached here.  /* #neg=1,582 #pos=154 */
%   | else return -0.10655045817131352;  // std dev = 17.570, 9,065.000 (wgt'ed) examples reached here.  /* #neg=8,745 #pos=320 */


% Clauses:

action(A, B, 0.7054345769056969) :- 
     facingleft(A), 
     visiblediver(A, C), 
     samelevelasdiver(A, C), 
     !. // Clause #1.

action(A, B, 0.28370242504373566) :- 
     facingleft(A), 
     !. // Clause #2.

action(A, B, -0.053141387481134846) :- 
     visibleenemy(A, C), 
     samelevelasenemy(A, C), 
     !. // Clause #3.

action(A, B, -0.10655045817131352) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.7054345769056969) :-  /* #neg=602 #pos=3,340 */ 
   facingleft(a),
   visiblediver(a, uniqueVar3),
   samelevelasdiver(a, uniqueVar3),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.28370242504373566) :-  /* #neg=6,798 #pos=5,036 */ 
   facingleft(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, -0.053141387481134846) :-  /* #neg=1,582 #pos=154 */ 
   visibleenemy(a, uniqueVar4),
   samelevelasenemy(a, uniqueVar4),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.10655045817131352) :-  /* #neg=8,745 #pos=320 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   visibleenemy(a, uniqueVar4)
%   samelevelasdiver(a, uniqueVar3)
%   facingleft(a)
%   visiblediver(a, uniqueVar3)
%   samelevelasenemy(a, uniqueVar4)
