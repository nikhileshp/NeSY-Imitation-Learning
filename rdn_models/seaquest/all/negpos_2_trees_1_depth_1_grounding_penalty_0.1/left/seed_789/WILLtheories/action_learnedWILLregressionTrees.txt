usePrologVariables: true.

% maxTreeDepthInNodes                 = 1
% maxTreeDepthInLiterals              = 12
% maxNumberOfLiteralsAtAnInteriorNode = 2
% maxFreeBridgersInBody               = 1
% maxNumberOfClauses                  = 20
% maxNodesToConsider                  = 10
% maxNodesToCreate                    = 10,000
% maxAcceptableNodeScoreToStop        = 0.003
% negPosRatio                         = 2.000
% testNegPosRatio                     = -1.000
% # of pos examples                   = 26,561
% # of neg examples                   = 0



%%%%%  WILL-Produced Tree #1 @ 22:49:01 11/28/25.  [Using 1,398,547,928 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( facingleft(A) )
%   then if ( diversnotfull(A) )
%   | then return 0.4404229150090942;  // std dev = 52.308, 11,249.000 (wgt'ed) examples reached here.  /* #neg=4,699 #pos=6,550 */
%   | else return 0.2760935448500222;  // std dev = 32.601, 4,369.000 (wgt'ed) examples reached here.  /* #neg=2,543 #pos=1,826 */
%   else if ( diversnotfull(A) )
%   | then return -0.08722864239605409;  // std dev = 19.446, 7,323.000 (wgt'ed) examples reached here.  /* #neg=6,923 #pos=400 */
%   | else return -0.12140907595021513;  // std dev = 8.514, 3,620.000 (wgt'ed) examples reached here.  /* #neg=3,546 #pos=74 */


% Clauses:

action(A, B, 0.4404229150090942) :- 
     facingleft(A), 
     diversnotfull(A), 
     !. // Clause #1.

action(A, B, 0.2760935448500222) :- 
     facingleft(A), 
     !. // Clause #2.

action(A, B, -0.08722864239605409) :- 
     diversnotfull(A), 
     !. // Clause #3.

action(A, B, -0.12140907595021513) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4404229150090942) :-  /* #neg=4,699 #pos=6,550 */ 
   facingleft(a),
   diversnotfull(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.2760935448500222) :-  /* #neg=2,543 #pos=1,826 */ 
   facingleft(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, -0.08722864239605409) :-  /* #neg=6,923 #pos=400 */ 
   diversnotfull(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.12140907595021513) :-  /* #neg=3,546 #pos=74 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingleft(a)
%   diversnotfull(a)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%  Final call for computing score for action.  %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

stepLength_tree1(1.0).

logPrior(-1.8).
action(D, E, Total) :- // A general accessor. 
   action(D, E, 1000000, Total), !.
action(D, E, Total) :- waitHere("This should not fail", action(D, E, Total)).

action(D, E, TreesToUse, Total) :- // A tree-limited accessor (e.g., for tuning the number of trees to use).
   logPrior(LogPrior),
   getScore_action_tree1(D, E, TreesToUse, Total1),
   Total is LogPrior + Total1,
   !.
action(D, E, TreesToUse, Total) :- waitHere("This should not fail", action(D, E, TreesToUse, Total)).

getScore_action_tree1(D, E, TreesToUse, 0.0) :- 1 > TreesToUse, !.
getScore_action_tree1(D, E, TreesToUse, Total1) :- action_tree1(D, E, Total), stepLength_tree1(StepLen), Total1 is Total * StepLen.

flattenedLiteralsInThisSetOfTrees(action, 2, [
   facingleft(a),
   diversnotfull(a)]).

%%%%%  WILL-Produced Tree Combined @ 22:49:35 11/28/25.  [Using 1,860,779,176 memory cells.]  %%%%%

% FOR action(A, B):
%   if ( facingleft(A) )
%   then if ( diversnotfull(A) )
%   | then return 0.4404229150090942;  // std dev = 52.308, 11,249.000 (wgt'ed) examples reached here.  /* #neg=4,699 #pos=6,550 */
%   | else return 0.2760935448500222;  // std dev = 32.601, 4,369.000 (wgt'ed) examples reached here.  /* #neg=2,543 #pos=1,826 */
%   else if ( diversnotfull(A) )
%   | then return -0.08722864239605409;  // std dev = 19.446, 7,323.000 (wgt'ed) examples reached here.  /* #neg=6,923 #pos=400 */
%   | else return -0.12140907595021513;  // std dev = 8.514, 3,620.000 (wgt'ed) examples reached here.  /* #neg=3,546 #pos=74 */


% Clauses:

action(A, B, 0.4404229150090942) :- 
     facingleft(A), 
     diversnotfull(A), 
     !. // Clause #1.

action(A, B, 0.2760935448500222) :- 
     facingleft(A), 
     !. // Clause #2.

action(A, B, -0.08722864239605409) :- 
     diversnotfull(A), 
     !. // Clause #3.

action(A, B, -0.12140907595021513) :- !. // Clause #4.


% The flattened versions of these clauses:

flattened_action(a, underscore, 0.4404229150090942) :-  /* #neg=4,699 #pos=6,550 */ 
   facingleft(a),
   diversnotfull(a),
   !. // Flattened version of clause #1.

flattened_action(a, underscore, 0.2760935448500222) :-  /* #neg=2,543 #pos=1,826 */ 
   facingleft(a),
   !. // Flattened version of clause #2.

flattened_action(a, underscore, -0.08722864239605409) :-  /* #neg=6,923 #pos=400 */ 
   diversnotfull(a),
   !. // Flattened version of clause #3.

flattened_action(underscore, underscore, -0.12140907595021513) :-  /* #neg=3,546 #pos=74 */ 
   !. // Flattened version of clause #4.


% The unique flattened literals:
%   facingleft(a)
%   diversnotfull(a)
